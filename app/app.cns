import React from 'react'
import ReactDOM from 'react-dom'
import { reactify } from './util/reactify'
import AppContainer from './components/AppContainer' :: reactify

ReactDOM.render(<AppContainer />, dom('#app-container'))
